import csv
from artist.models import artist
import os

filepath=r'D:\ai\Downloads\Data\paintings\data'

with open(os.path.join(filepath,'artist.csv'), newline='', encoding='utf-8') as csvfile:
	reader = csv.DictReader(csvfile)
	for row in reader:
		artist.objects.create(
			artist=row['style']
		)

print('완료')